# Flex_Cup_Compiler
School Project: This app uses libraries like flex and cup to develop a compiler that compiles C code. It performs lexical analysis and outputs a token table, three-way code, and Polish notation. It can also save the compiled code as a .cmx file.
Libraries in the lib folder
